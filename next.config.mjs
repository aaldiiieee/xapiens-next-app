const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "reqres.in",
      },
      {
        hostname: "lh3.googleusercontent.com",
      },
    ],
  },
};

export default nextConfig;
